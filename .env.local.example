# 复制本文件为 .env.local，按自己机器的情况改。
# .env.local 已被 .gitignore 忽略，不会被提交，也不会影响其他同事。
#
# 典型用途：把接口代理指向自己本地的后端服务或局域网联调地址，
# 这样每个人都能独立联调，不用改 .env.development 去污染别人的环境。

# 本地后端 / 局域网联调地址
VITE_PROXY_TARGET=http://127.0.0.1:8080

# 端口被占用时改这里
VITE_DEV_PORT=5173
