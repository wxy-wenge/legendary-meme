# 复制本文件为 .env.development.local，按自己机器的情况改。
# 该文件已被 .gitignore 忽略（.env.*.local），不会提交，也不会影响同事。
#
# 文件名必须是 .env.<mode>.local —— vite 的加载顺序是：
#   .env  <  .env.local  <  .env.<mode>  <  .env.<mode>.local
# 后面的覆盖前面的，所以写进 .env.local 会被 .env.development 压掉、不生效。
#
# 个人覆盖统一用 .env.development.local（开发环境）/ .env.production.local（生产构建）。

# 后端接口地址。小程序不走 vite 代理，必须写绝对地址；
# 真机调试时把 127.0.0.1 换成电脑的局域网 IP。
VITE_API_BASE_URL=http://127.0.0.1:8080

# 后端已就绪，关掉前端模拟层（src/api/mock.ts）。
VITE_USE_MOCK=false

# 以下是 H5 走 vite proxy 时用的：需要时取消注释，并把上面的
# VITE_API_BASE_URL 改回 /api。
# VITE_PROXY_TARGET=http://127.0.0.1:8080
# VITE_DEV_PORT=5173
